---
import Layout from '../layouts/Layout.astro';
import IntakeFlow from '../components/islands/IntakeFlow';
---

<Layout title="Start Your Project — 52Eighty Creative">
  {/* The flow presents as a sequence of full-height screens and owns its own
      intro/heading, so there's no page-level header or reading-column cap here
      — each step manages its own width via `StepShell`. */}
  <IntakeFlow client:load />
</Layout>
